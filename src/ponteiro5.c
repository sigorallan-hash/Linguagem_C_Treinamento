/*
tipos de armazenamento de dados usando porcentagem com variaveis
tipos de variaveis:
chair =
int =
float =

%:
d = numeros inteiros.
f = numeros flutuantes(decimais).
c = um character.
s = uma cadeia de characters(caracteres).
*/
#include <stdio.h>
#include <stdlib.h>

int main()
{
    system
    ("clear");

    char 
    primeiro_nome[10];

    printf
    ("Digite o seu nome\n");
    scanf
    ("%s\n",primeiro_nome);
    printf
    ("Olá Sr(a). %s\n",primeiro_nome);

    return 0;
}