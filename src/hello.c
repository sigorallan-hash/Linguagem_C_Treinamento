//primeiro arquivo em linguagem c
//vamos importar a biblioteca base para executar o nosso programa
//a biblioteca se chama studio (std->standard) | i-> input | o -> input)
//para importar, usaremos o comando #include
#include <stdio.h>

//abaixo, iremos escrever os comandos que serão realmente executados
//utilizaremos a função main(principal)para iniciar a execução
int main(){
 //o comando printf é para exibir o comando na tela do console ou terminal
 printf("hello world\n!");
 //informar ao nosso compilador(tradutor) gcc que o programa acabouls 
 return 0;

}